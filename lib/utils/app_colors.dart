import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors (based on image)
  static const Color primary = Color(0xFF1A2B4A);           // Navy Blue
  static const Color accent = Color(0xFFFF8C42);            // Orange
  static const Color secondaryBackground = Color(0xFFD4E09B); // Pale Yellow-Green
  static const Color background = Color(0xFFF5F5F5);        // Light Gray Background

  // Extra for feature box
  static const Color lightGray = Color(0xFFEFEFEF);         // Card background in screenshot

  // Legacy aliases
  static const Color navyBlue = primary;
  static const Color accentPrimary = accent;
  static const Color accentSecondary = secondaryBackground;
  static const Color primaryBackground = background;

  // Text Colors
  static const Color textDark = Color(0xFF2C3E50);
  static const Color textLight = Color(0xFF7F8C8D);
  static const Color textSecondary = Color(0xFF95A5A6);
  static const Color textOnAccent = Color(0xFFFFFFFF);

  // Basic
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color orange = accent;

}
